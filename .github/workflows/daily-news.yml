      - name: Run Python code
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          import os
          import requests
          # ဒီနေရာမှာ space တွေ အတိအကျမှန်ဖို့ လိုပါတယ်...
          def send_news():
              # ... ကျန်တဲ့ code များ ...
          send_news()
